---
"@aceshooting/lyra-ui": minor
---

`lyra-empty`'s `compact` mode gains a `--lyra-empty-compact-align` custom property (defaulting to today's exact `flex-start`/`start` pair) so a consumer can combine `compact`'s denser padding with a centered heading/description layout by setting it to `center`.
