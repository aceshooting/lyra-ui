---
"@aceshooting/lyra-ui": minor
---

Add `lyra-button`, a generic action-button primitive (`variant`/`appearance`/`size`/`loading`/`disabled`/`type`, default + `start`/`end` slots) -- the `lyra-*` equivalent of a plain `wa-button`.
