---
"@aceshooting/lyra-ui": minor
---

Add `lr-menubar` and `lr-menubar-item`, an APG menubar whose items drop down slotted `lr-menu`s, with roving focus, RTL-aware arrow traversal that carries the open menu along, hover switching, and typeahead. Items wrap at narrow allocations, preserve native Tab behavior, and support card and plain frames.
