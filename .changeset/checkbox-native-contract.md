---
'@aceshooting/lyra-ui': patch
---

Align `lyra-checkbox` with the native checkbox keyboard, focus, reset, ARIA-state, and `input`/`change` event contracts while retaining `lyra-change` as a compatibility alias.
