---
"@aceshooting/lyra-ui": minor
---

`lyra-typing-indicator`'s dots-variant stagger delays are now themeable via `--lyra-typing-dot-stagger-1`/`-2` (defaulting to today's exact `600ms`/`1200ms`), so a consumer retiming `--lyra-transition-ambient` can keep the stagger proportional.
