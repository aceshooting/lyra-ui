---
"@aceshooting/lyra-ui": patch
---

Fix `lr-token-input`'s draft-input and inline token-editor leaving `::placeholder` at the browser's
default color, and add missing `:hover`/`:focus-visible` to the per-token remove button.
