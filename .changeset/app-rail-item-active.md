---
"@aceshooting/lyra-ui": minor
---

`lyra-app-rail-item`: add an `active` property that reflects `aria-current="page"` onto the
internal link/button, mirroring `lyra-conversation-item`'s existing `active` pattern.
