---
"@aceshooting/lyra-ui": minor
---

`<lyra-file-icon>` gains a `size` property (bytes, formatted via the same convention as `<lyra-attachment-chip>`) shown alongside its label, and exposes the raw MIME type as a `title` tooltip.
