/**
 * Resolves the URL of a flag PNG shipped in this package.
 * @param code ISO 3166-1 alpha-2 country/territory code, lowercase (e.g. `fr`, `us`).
 */
export declare function flagUrl(code: string): string;
